import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Multiple-Choice-App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
